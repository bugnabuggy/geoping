import IGEOPoint from '../DTO/types/checkList/geoPoint';

export const addNewPoint = ( newPoint: IGEOPoint ) => ( dispatch: Function ) => {
  return '';
};

export const checkGEOPosition = () => ( dispatch: Function ) => {
  return '';
};

export const loadGEOPointsList = () => ( dispatch: Function ) => {
  return '';
};

export const deleteGEOPoint = ( idGEOPoint: number ) => ( dispatch: Function ) => {
  return '';
};

export const editGEOPoint = ( idGEOPoint: number ) => ( dispatch: Function ) => {
  return '';
};

export const loadCheckList = ( idCheckList: number ) => ( dispatch: Function ) => {
  return '';
};