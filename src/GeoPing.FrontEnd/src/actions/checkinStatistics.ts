/* Select */
export const selectList = () => ( dispatch: Function ) => {
  return '';
};

export const selectUser = () => ( dispatch: Function ) => {
  return '';
};

export const selectPeriod = () => ( dispatch: Function ) => {
  return '';
};

/* may be*/
export const loadTableData = () => ( dispatch: Function ) => {
  return '';
};

/* Load */
export const loadLists = () => ( dispatch: Function ) => {
  return '';
};

export const loadUsers = () => ( dispatch: Function ) => {
  return '';
};
