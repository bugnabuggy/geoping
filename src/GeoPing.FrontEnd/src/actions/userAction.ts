import IRegistrationUserType from '../DTO/types/userAction/registrationUserDataType';

export const authorizationUser = ( email: string, password: string ) => ( dispatch: Function ) => {

  return '';
};

export const registrationUser = ( registrationUserData: IRegistrationUserType ) => ( dispatch: Function ) => {

  return '';
};

export const resetPasswordEnterLoginOrEmail = ( emailOrLogin: string ) => ( dispatch: Function ) => {
  return '';
};

export const resetPasswordEnterNewPassword = ( newPassword: string ) => ( dispatch: Function ) => {
  return '';
};
